/**
 * config.js — Configuración global del frontend
 *
 * En producción (Vercel), la variable __API_URL__ es inyectada
 * automáticamente por el script de build definido en vercel.json.
 *
 * En desarrollo local, se usa el fallback: http://localhost:3000
 */

const API_URL = "http://localhost:3000";
